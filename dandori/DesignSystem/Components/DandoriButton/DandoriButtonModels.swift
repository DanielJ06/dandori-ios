import SwiftUI

// MARK: - DandoriButton Models

enum DandoriButtonVariant {
    case primary
    case secondary
    case tertiary
    case ghost
}

enum DandoriButtonSize {
    case small
    case medium
    case large
    case fullWidth
}

enum DandoriButtonState {
    case enabled
    case disabled
    case loading
}
